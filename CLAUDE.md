# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — it contains no source code, no README, and no package manifest, and it is not yet a git repository. The only existing file is `.claude/settings.json`, which sets `plansDirectory` to `.claude/plans/`.

There are no build, lint, or test commands to document yet. Update this file once the codebase takes shape (language/framework, build tooling, and architecture).
